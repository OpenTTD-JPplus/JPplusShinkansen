 /* Panto in middle */

	switch (FEAT_TRAINS, SELF, sw_l0_series_panto_in_middle, CarNumberFromEnd() - CarNumber() ) {
		0..1:  sw_l0_series_coach2;
		sw_l0_series_coach1;
	}

 /* Penultimate Pantos Dual */

	switch (FEAT_TRAINS, SELF, sw_l0_series_penultimate_pantos, CarNumber() ) {
		2:  sw_l0_series_coach2;
		sw_l0_series_coach1;
	}

	switch (FEAT_TRAINS, SELF, sw_l0_series_penultimate_pantos_dual, CarNumber() == 2 || CarNumberFromEnd() == 2 ) {
		1:  sw_l0_series_penultimate_pantos;
		sw_l0_series_coach1;
	}

/* Midcar selection */

	switch (FEAT_TRAINS, SELF, sw_l0_series_midcars, NumberCarsInConsist() ) {
		0..5: sw_l0_series_panto_in_middle;
		default: sw_l0_series_penultimate_pantos_dual;
	}

/* Cab selection */

/* Find the cabs */

	switch (FEAT_TRAINS, SELF, sw_l0_series_cabs, position_in_consist) {
		0..2: sw_l0_series_cab001;
		sw_l0_series_cab002;
	}

	switch (FEAT_TRAINS, SELF, sw_l0_series_graphics, IsCab() ) {
		1:  sw_l0_series_cabs;
		sw_l0_series_midcars;
	}
